from django.urls import path
from . import views

app_name = 'appointment_reservation'

urlpatterns = [
    # path('make_appointment/', views.MakeAppointmentView.as_view(), name='make_appointment'),
]